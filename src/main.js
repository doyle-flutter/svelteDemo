// import App from './App.svelte';
import CunstomDrawer from './CunstomDrawer/CunstomDrawer.svelte';

// const app = new App({target: document.body});
const app = new CunstomDrawer({target: document.body});
export default app;